# Server Configuration
server.port=9090

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/appointment_booking?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root

# JPA Configuration - SIMPLE KARO
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable foreign key checks for initial setup
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# Spring Security (Temporarily disabled for development)
spring.security.enabled=false

# JSON Serialization Configuration - CORRECT PROPERTIES
spring.jackson.default-property-inclusion=non_null
spring.jackson.deserialization.fail-on-unknown-properties=false

# For Jackson 2.15+ use these:
spring.jackson.stream-read-constraints.max-nesting-depth=2000
spring.jackson.stream-write-constraints.max-nesting-depth=2000

# Alternative: Use system property
# -Djackson.streamReadConstraints.maxNestingDepth=2000

# Disable Spring Security auto-configuration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration


# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=reminder.appointment.project@gmail.com
spring.mail.password=rvru udzf kvco gcux
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email Templates
spring.thymeleaf.prefix=classpath:/templates/email/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8


# Twilio Configuration
twilio.account.sid=AC98d52026258a1ab901b81eda439c0a3b
twilio.auth.token=4a0a4ac99b843c27939970e4db449c21
twilio.phone.number=+15076088039

